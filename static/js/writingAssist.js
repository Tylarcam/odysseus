/**
 * Local writing assist (Harper / grammar hints near the composer).
 * Stubbed after the source file was wiped — keeps static/app.js importable.
 * Native browser spellcheck remains enabled.
 */
export function wireWritingAssist(_textarea, _opts = {}) {
  // no-op until the full assist module is restored
}

export default { wireWritingAssist };
